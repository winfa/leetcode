/*
 * @lc app=leetcode id=212 lang=typescript
 *
 * [212] Word Search II
 */

// @lc code=start
function findWords(board: string[][], words: string[]): string[] {

};

function getWordsFromBoard(board: string[][]) {

}

function getWordsStartFromElement({ hIndex, vIndex }: { hIndex: number, vIndex: number }, board: number[][] ) {
  const hLength = board[0].length;
  const vLength = board.length;

  let nextElement = board[vIndex][hIndex];
  while(nextElement !== undefined) {
    
  }

}
// @lc code=end

